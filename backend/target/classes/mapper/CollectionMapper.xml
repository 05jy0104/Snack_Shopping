<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snack.mapper.CollectionMapper">
    <resultMap id="BaseResultMap" type="com.snack.entity.UserCollection">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="snack_id" property="snackId"/>
        <result column="snack_name" property="snackName"/>
        <result column="image" property="image"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM collection WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findByUserIdAndSnackId" resultMap="BaseResultMap">
        SELECT * FROM collection WHERE user_id = #{userId} AND snack_id = #{snackId}
    </select>

    <insert id="insert" parameterType="com.snack.entity.UserCollection" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO collection (user_id, snack_id, snack_name, image, create_time)
        VALUES (#{userId}, #{snackId}, #{snackName}, #{image}, #{createTime})
    </insert>

    <delete id="delete">
        DELETE FROM collection WHERE id = #{id}
    </delete>

    <delete id="deleteByUserIdAndSnackId">
        DELETE FROM collection WHERE user_id = #{userId} AND snack_id = #{snackId}
    </delete>
</mapper>
