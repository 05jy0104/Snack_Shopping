<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snack.mapper.OrderMapper">
    <resultMap id="BaseResultMap" type="com.snack.entity.Order">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="order_no" property="orderNo"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="address" property="address"/>
        <result column="phone" property="phone"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM orders ORDER BY create_time DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="findByOrderNo" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>

    <insert id="insert" parameterType="com.snack.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, username, order_no, total_amount, status, address, phone, create_time)
        VALUES (#{userId}, #{username}, #{orderNo}, #{totalAmount}, #{status}, #{address}, #{phone}, #{createTime})
    </insert>

    <update id="update" parameterType="com.snack.entity.Order">
        UPDATE orders
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="address != null">address = #{address},</if>
            <if test="phone != null">phone = #{phone},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM orders WHERE id = #{id}
    </delete>
</mapper>
