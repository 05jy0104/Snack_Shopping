<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snack.mapper.SnackMapper">
    <resultMap id="BaseResultMap" type="com.snack.entity.Snack">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
        <result column="price" property="price"/>
        <result column="image" property="image"/>
        <result column="description" property="description"/>
        <result column="stock" property="stock"/>
        <result column="sales" property="sales"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM snack ORDER BY create_time DESC
    </select>

    <select id="findByCategoryId" resultMap="BaseResultMap">
        SELECT * FROM snack WHERE category_id = #{categoryId} ORDER BY create_time DESC
    </select>

    <select id="search" resultMap="BaseResultMap">
        SELECT * FROM snack WHERE name LIKE CONCAT('%', #{keyword}, '%') ORDER BY create_time DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM snack WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.snack.entity.Snack" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO snack (name, category_id, category_name, price, image, description, stock, sales, create_time)
        VALUES (#{name}, #{categoryId}, #{categoryName}, #{price}, #{image}, #{description}, #{stock}, #{sales}, #{createTime})
    </insert>

    <update id="update" parameterType="com.snack.entity.Snack">
        UPDATE snack
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null">image = #{image},</if>
            <if test="description != null">description = #{description},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="sales != null">sales = #{sales},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM snack WHERE id = #{id}
    </delete>

    <update id="updateStock">
        UPDATE snack SET stock = stock - #{quantity}, sales = sales + #{quantity} WHERE id = #{id}
    </update>
</mapper>
